// Preload script — runs in a sandboxed context with access to both
// the DOM APIs and a safe subset of Node.js APIs.
// Expose any secure bridge APIs here using contextBridge if needed in the future.

// Example (currently empty):
// import { contextBridge, ipcRenderer } from 'electron'
// contextBridge.exposeInMainWorld('electronAPI', { ... })
